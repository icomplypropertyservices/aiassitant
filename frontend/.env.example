# Public API base URL (no trailing slash).
# Local:
# VITE_API_URL=http://localhost:8000
#
# Production (your FastAPI host — NOT Vercel; Vercel only serves this SPA):
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=http://localhost:8000
